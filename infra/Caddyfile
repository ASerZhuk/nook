# Один домен: web push и PWA требуют HTTPS — Caddy выпускает сертификат автоматически
{$APP_DOMAIN} {
	encode gzip
	handle /api/* {
		reverse_proxy api:8000
	}
	handle {
		reverse_proxy web:3000
	}
}
